# 🔍 نظام كشف المخاطر المتقدم (Risk Detection System)

## نظرة عامة

تم تطوير نظام متقدم لكشف المخاطر في تطبيق YouMatter، يتجاوز النظام البسيط السابق الذي اعتمد على كلمات مفتاحية فقط.

---

## 📊 المكونات الرئيسية

### 1. **RiskLevel Enum** 
الموجود في: `lib/models/risk_assessment.dart`

```dart
enum RiskLevel {
  low,      // لا يوجد خطر
  medium,   // خطر منخفض (قلق، ضغط نفسي)
  high,     // خطر عالي (إشارات لإيذاء النفس)
  critical, // خطر حرج (إشارات واضحة للانتحار)
}
```

### 2. **RiskAssessment Model**
نموذج يحتوي على تفاصيل كاملة للتحليل:

```dart
class RiskAssessment {
  final RiskLevel level;              // مستوى الخطر
  final double confidence;            // درجة الثقة (0-100)
  final String reason;                // سبب التصنيف
  final List<String> detectedKeywords;// الكلمات المفتاحية المكتشفة
  final List<String> recommendations; // التوصيات
  final DateTime timestamp;           // وقت التحليل
  final String originalText;          // النص الأصلي
}
```

### 3. **RiskEngine**
الموجود في: `lib/services/risk_engine.dart`

محرك التحليل المتقدم الذي يستخدم:
- كلمات مفتاحية على مستويات مختلفة
- نظام تصنيف بناءً على درجات
- تحليل الكلمات الإيجابية (التي تقلل من الخطر)
- حساب درجة الثقة في التحليل

---

## 🎯 كيفية عمل النظام

### المرحلة 1: التحليل النصي
```
النص المُدخل → تحويل لأحرف صغيرة → البحث عن الكلمات المفتاحية
```

### المرحلة 2: حساب درجة الخطر
```
الكلمات الحرجة:     50 نقطة لكل كلمة
الكلمات العالية:   30 نقطة لكل كلمة
الكلمات المتوسطة:  15 نقطة لكل كلمة
الكلمات الإيجابية: -20 نقطة لكل كلمة
```

### المرحلة 3: تحديد المستوى
```
النقاط ≥ 50  → خطر حرج (CRITICAL)
النقاط ≥ 35  → خطر عالي (HIGH)
النقاط ≥ 15  → خطر متوسط (MEDIUM)
النقاط < 15  → لا يوجد خطر (LOW)
```

---

## 📚 الكلمات المفتاحية المستخدمة

### 🚨 الكلمات الحرجة (Critical Keywords)
تحتوي على إشارات واضحة للانتحار أو الإيذاء الفوري:

```
انتحار، suicide، أنهي حياتي، end my life
سأقتل نفسي، i will kill myself، أريد أن أموت
i want to die، أنا سأموت، ending it
أذي نفسي، harm myself، self harm، cut myself
overdose، جرعة زائدة، الشنق، hanging
سم، poison، قفز، jump، سقوط
```

### ⚠️ الكلمات العالية (High Risk Keywords)
تحتوي على إشارات لإيذاء النفس أو اليأس الشديد:

```
أذي، hurt، pain، ألم، ألم شديد
لا أستطيع، can't take it، لا أتحمل
يائس، hopeless، depressed، مكتئب
وحيد، alone، lonely، وحدة، لا أحد يفهمني
فقدت الأمل، lost hope، ليس هناك نقطة
لا فائدة، worthless، useless، بلا قيمة
عبء، burden، حمل، problem، مشكلة
أكره، hate، أكره حياتي
```

### 🟡 الكلمات المتوسطة (Medium Risk Keywords)
تحتوي على إشارات لقلق أو ضغط نفسي:

```
قلق، anxiety، worried، قلق جداً
تعب، tired، متعب، exhausted، منهك
حزن، sad، حزين جداً، crying
بكاء، tears، دموع، stressed، مجهد
ضغط، pressure، نسيان، forget everything
فشل، failure، failed، فشلت
```

### ✨ الكلمات الإيجابية (Positive Keywords)
تقلل من درجة الخطر:

```
أفضل، better، تحسن، improved
سعيد، happy، مسرور، glad
شكرا، thanks، أشكرك، شكراً لك
أمل، hope، متفائل، optimistic
تحب، love، أحب، يحب
```

---

## 🔄 سير العملية في التطبيق

### عند إرسال المستخدم رسالة:

```
1️⃣ المستخدم يكتب الرسالة
   ↓
2️⃣ يضغط على زر الإرسال
   ↓
3️⃣ تُحفظ الرسالة في الواجهة
   ↓
4️⃣ RiskEngine.analyzeText() يحلل الرسالة
   ↓
5️⃣ يتم حفظ التحليل في Firestore
   ↓
6️⃣ بناءً على المستوى:
   ├─ CRITICAL → عرض تنبيه حرج + إرسال SMS
   ├─ HIGH → عرض تنبيه عالي + إرسال SMS (إذا كان وضع الطوارئ)
   ├─ MEDIUM → عرض SnackBar تحذيري
   └─ LOW → لا يوجد تنبيه
   ↓
7️⃣ جلب الرد من Gemini API
   ↓
8️⃣ عرض الرد للمستخدم
```

---

## 🛡️ الإجراءات المتخذة بناءً على المستوى

### 🚨 CRITICAL - خطر حرج
```
1. عرض dialag تنبيه مرعب بخلفية حمراء
2. عرض رسالة "خطر حرج - تنبيه طوارئ"
3. عرض التوصيات المقترحة
4. إرسال SMS فوري لجهات الاتصال الموثوقة
5. حفظ البيانات للمراجعة
```

**التوصيات:**
- 📞 اتصل بجهة موثوقة فوراً
- 🆘 تواصل مع خط الأزمات النفسية
- 💊 فكر في طلب مساعدة متخصص
- 🤝 شارك مشاعرك مع شخص تثق به

### ⚠️ HIGH - خطر عالي
```
1. عرض dialog تنبيه برتقالي
2. عرض رسالة توضح الخطر
3. عرض أول 3 توصيات
4. إذا كان وضع الطوارئ: إرسال SMS
5. حفظ البيانات
```

**التوصيات:**
- 🤝 تحدث مع جهة موثوقة عما تشعر به
- 🧘 جرب تقنيات الاسترخاء والتنفس
- 📝 اكتب مشاعرك في دفتر يوميات
- 💬 ابق على اتصال مع المساعدين

### 🟡 MEDIUM - خطر متوسط
```
1. عرض SnackBar تحذيري
2. عرض رسالة الملاحظة
3. عدم إرسال SMS (لا يوجد طوارئ فورية)
4. حفظ البيانات
```

### ✅ LOW - آمن
```
- لا يوجد تنبيه
- الدردشة تستمر عادية
- حفظ البيانات للإحصائيات
```

---

## 💾 حفظ البيانات

يتم حفظ التحليل في Firestore تحت:
```
users/
  {userId}/
    riskAssessments/
      {timestamp}:
        {
          level: "high",
          confidence: 85.5,
          reason: "تم كشف مؤشرات خطر عالية",
          detectedKeywords: ["أذي", "ألم"],
          recommendations: [...],
          timestamp: "2026-05-06T12:30:00",
        }
```

---

## 🎨 الألوان والرموز

| المستوى | اللون | الرمز | المعنى |
|--------|-------|-------|--------|
| CRITICAL | 🔴 أحمر (#FF6B6B) | 🚨 | خطر حرج فوري |
| HIGH | 🟠 برتقالي (#FF9800) | ⚠️ | خطر عالي |
| MEDIUM | 🟡 أصفر | ⚡ | ضغط نفسي |
| LOW | 🟢 أخضر | ✅ | آمن |

---

## 🔐 نقاط أمان هامة

1. **لا تُحذف البيانات المكتشفة تلقائياً**
   - يتم حفظها كاملة للمراجعة

2. **الكلمات المفتاحية تُحدّث دورياً**
   - يمكن إضافة كلمات جديدة بناءً على الخبرة

3. **معالجة الأخطاء شاملة**
   - أي خطأ في الإرسال لن يُوقف التطبيق

4. **الخصوصية محمية**
   - النص الأصلي يُحفظ مشفر في Firestore (يجب تفعيل التشفير)

---

## 🚀 الميزات المستقبلية المقترحة

1. **تحليل المشاعر باستخدام ML**
   - استخدام نماذج تعلم الآلة لتحليل أعمق

2. **السياق التاريخي**
   - تحليل تاريخ الرسائل السابقة

3. **نماذج صحية موحدة**
   - استخدام PHQ-9, GAD-7 لتقييم الحالة

4. **التنبيهات الذكية**
   - تنبيهات مخصصة بناءً على تاريخ المستخدم

5. **تقارير دورية**
   - تقارير صحية أسبوعية/شهرية للمستخدم

---

## 📞 استدعاء الدوال

### استخدام بسيط:
```dart
String userMessage = "أنا لا أستطيع تحمل هذا";
RiskAssessment assessment = RiskEngine.analyzeText(userMessage);

print(assessment.level);          // RiskLevel.high
print(assessment.reason);         // ⚠️ تم كشف مؤشرات...
print(assessment.confidence);     // 78.5
print(assessment.detectedKeywords); // ["لا أستطيع", "تحمل"]
```

### استخدام متقدم:
```dart
RiskAssessment assessment = RiskEngine.analyzeText(userMessage);

// معالجة حسب المستوى
if (assessment.level == RiskLevel.critical) {
  // تنفيذ إجراءات الطوارئ
} else if (assessment.level == RiskLevel.high) {
  // تنفيذ إجراءات عالية
}

// الوصول إلى البيانات
assessment.toMap(); // تحويل إلى Map للحفظ في Firestore
```

---

## ✅ قائمة الفحص للاختبار

- [ ] اختبار كلمات حرجة → يجب أن تُعطي CRITICAL
- [ ] اختبار كلمات عالية → يجب أن تُعطي HIGH
- [ ] اختبار كلمات متوسطة → يجب أن تُعطي MEDIUM
- [ ] اختبار كلمات موجبة → يجب أن تقلل الدرجة
- [ ] اختبار الرسائل العادية → يجب أن تُعطي LOW
- [ ] اختبار الخليط (موجب + سلبي) → يجب التوازن الصحيح
- [ ] اختبار حفظ البيانات → التأكد من الحفظ
- [ ] اختبار إرسال SMS → التأكد من الوصول

---

**تم إنشاء هذا النظام:** يونيو 2026
**النسخة:** 2.0 (محسّنة)
**الحالة:** ✅ جاهز للاستخدام
